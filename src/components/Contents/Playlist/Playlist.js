import { styled } from "styled-components";

const PlayList = () => {
  return <PlayListContainer>PlayList</PlayListContainer>;
};

export default PlayList;

const PlayListContainer = styled.div`
  flex: 1;
  box-sizing: border-box;
  border: 1px solid #000;
  height: 1000px;
`;
